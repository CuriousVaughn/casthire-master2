%section#hero-section-clock.hero-section.text-center
  .container
    = form_tag sessions_path, :class => 'highlight-box', :id => 'sign-in-form' do |f|
      %h2 Sign in to CastHire
      %p
        = text_field_tag :email, nil, :placeholder => 'email'
      %p
        = password_field_tag :password, nil, :placeholder => 'password'
      = submit_tag 'Sign In', :class => 'btn extra-spacing-vertical'
      -#= link_to 'Forgot Your Password?', new_password_reset_path
      %p.text-center
        // hide create new account for now thanks to USC feedback.
        // = link_to 'Create New Account', new_user_path
